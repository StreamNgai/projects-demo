<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.weihl.amazing">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".business.main.MainActivity" />
        <activity
            android:name=".business.start.StartActivity"
            android:theme="@style/Start.Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".business.account.LoginActivity"
            android:theme="@style/Account.Theme" />
        <activity
            android:name=".business.account.RetrievePasswordActivity"
            android:theme="@style/Account.Theme" />
        <activity
            android:name=".business.account.RegisterActivity"
            android:theme="@style/Account.Theme" />
        <activity
            android:name=".business.browse.AlbumActivity"
            android:theme="@style/Browse.Theme" />
        <activity android:name=".business.favorite.FavoriteActivity" />
        <activity android:name=".business.account.AccountActivity" />
    </application>

</manifest>